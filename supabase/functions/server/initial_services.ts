/**
 * Initial service menu seed data
 * 
 * This file provides default services when the KV store is empty.
 * Returns an empty object to ensure the system starts with a clean slate.
 * 
 * If you need to reset the service menu:
 * 1. Clear the KV store key: settings:service-menu
 * 2. Backend will auto-seed this empty structure
 * 3. Add services manually through Admin UI
 */
export const initialServices = {};
